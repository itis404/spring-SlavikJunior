# Скопируй этот файл в application-secret.yml и заполни значения.
# application-secret.yml добавлен в .gitignore — не коммить его.
# Используется только для локального запуска: ./gradlew bootRun

app:
  jwt:
    secret: "замени-на-случайную-строку-минимум-32-символа"
  tbank:
    terminal-key: ""
    secret-key: ""
  firebase:
    service-account-base64: ""
