# Dev infrastructure only — Redis for local development
# Usage: docker-compose -f docker-compose-dev.yml up -d
version: "3.9"

services:
  redis:
    image: redis:7-alpine
    container_name: coffee-shop-redis-dev
    ports:
      - "6379:6379"
    restart: unless-stopped
